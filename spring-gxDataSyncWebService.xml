<?xml version="1.0" encoding="UTF-8"?>    
<beans xmlns="http://www.springframework.org/schema/beans"  
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"  
    xmlns:http-conf="http://cxf.apache.org/transports/http/configuration" 
    xmlns:aop="http://www.springframework.org/schema/aop"
	xmlns:tx="http://www.springframework.org/schema/tx"
    xmlns:jaxws="http://cxf.apache.org/jaxws"  
    xmlns:sec="http://www.springframework.org/schema/security"
    xmlns:task="http://www.springframework.org/schema/task"       
    xsi:schemaLocation="  
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd 
		http://cxf.apache.org/transports/http/configuration http://cxf.apache.org/schemas/configuration/http-conf.xsd 
		http://cxf.apache.org/jaxws http://cxf.apache.org/schemas/jaxws.xsd
		http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-3.0.xsd
		http://www.springframework.org/schema/task http://www.springframework.org/schema/task/spring-task-3.0.xsd">  
  
    <import resource="classpath:META-INF/cxf/cxf.xml" />  
    <import resource="classpath:META-INF/cxf/cxf-extension-soap.xml" />  
    <import resource="classpath:META-INF/cxf/cxf-servlet.xml" />
    
    <!-- 在服务器端设置响应超时限制，现在使用的是默认值60秒 -->
    <http-conf:destination name="*.http-destination">
      <http-conf:server ReceiveTimeout="60000"/> 
    </http-conf:destination>


	<!-- 养老同步接口service  -->
	<bean id="gxDataSyncCallServiceImpl" parent="transactionProxy">
		<property name="target">
			<bean class="com.yinhai.gxdatasync.server.impl.GXDataSyncCallServiceImpl" parent="baseService">
			</bean>
		</property>
	</bean>

	<jaxws:endpoint id="gxDataSyncCallService"  implementor="#gxDataSyncCallServiceImpl" address="/gxDataSyncCallService">
		<!-- 添加了两个In拦截器 -->

		<jaxws:inInterceptors>
			<bean class="org.apache.cxf.interceptor.LoggingInInterceptor"/><!-- 嵌套Bean，创建一个Bean -->
			<bean class="com.yinhai.gxdatasync.util.AuthInterceptor"/>
			<!-- <ref bean="anotherInterceptor">引用一个已有的Bean -->
		</jaxws:inInterceptors>
		<!-- 需要配置Out拦截器，使用<jaxws:outInterceptors> -->
	</jaxws:endpoint>

</beans>